import {StyleSheet} from 'react-native';
import {ITaskListItemProps} from './interface';

const getStyling = (props: ITaskListItemProps) => {
  return StyleSheet.create({});
};

export default getStyling;
